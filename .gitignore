data/*.csv
data/*.npy

# submissions: track only the final ensemble; ignore intermediate ones
submissions/*
!submissions/ensemble.csv

# preds: track all softmax probabilities (so teammates can ensemble / meta-model)
# (nothing to ignore here)

.venv/
__pycache__/
.ipynb_checkpoints/
.DS_Store

# LaTeX build artifacts
report/*.aux
report/*.log
report/*.out
report/*.bbl
report/*.blg
report/*.synctex.gz
report/*.fdb_latexmk
report/*.fls
report/*.pdf
